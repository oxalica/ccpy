#ifndef __CCPY_UTIL_TYPES__
#define __CCPY_UTIL_TYPES__

#include <cstdint>
#include <string>

namespace ccpy {

using Integer = std::int64_t;
using Decimal = double;

using Str = std::string;
using StrPos = const char *;

} // namespace ccpy

#endif // __CCPY_UTIL_TYPES__
